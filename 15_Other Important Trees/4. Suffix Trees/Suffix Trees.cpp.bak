#include<iostream>
using namespace std;

struct node {
	int val;
	vector<node*> edges(26, NULL);
};

int main() {
	string s;
	s = "xabxacy";
	for(int i=0; i<s.size(); i++) {
		int idx = s[i]-'a';
		if(node->edges[idx]!=NULL) 


	return 0;
}